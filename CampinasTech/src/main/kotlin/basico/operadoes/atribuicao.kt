package basico.operadoes

fun main(){
    var a : Int = 5
    var b : Int = 3

    println(a)

    // visual cont = cont + 1
    //visual MOD  5 MOD 3 = 2
    // b+=a // b = b + a
    // b-=a // b = b - a
    // b*=a // b = b * a
    // b/=a // b = b / a
    // a%b // a = a % b

}